# Agent Engineering Kit（AEK）Overview

> **让 Coding Agent 不只是“会写代码”，而是能够理解存量项目、遵守工程规则、基于证据完成迭代与排障，并持续积累项目知识。**

Agent Engineering Kit（AEK）是一套面向 **AI Coding Agent 与真实软件工程协作** 的仓库级工程控制体系。

AEK 不替代 Codex、Claude Code 等 Coding Agent，也不尝试重新实现一个 Agent Runtime。

它关注的是另一个问题：

> 当 AI 真正开始参与需求分析、方案设计、编码、测试、问题定位和交付之后，如何保证它理解的是正确的项目上下文，修改的是正确的范围，产出的结果可以验证、可以追踪、可以交接，并且不会在下一次会话中重新从零开始？

AEK 围绕这个问题提供：

- 项目上下文恢复；

- 分级工程路线；

- 需求、设计、测试与任务追踪；

- AI Review 与确定性 Gate；

- 证据驱动的异常定位；

- 可语义检索的项目长期记忆；

- TDD 与安全审查；

- 交付验证与 As-Built 收口；

- 项目级统一安装和治理。

AEK 当前尤其面向：

> **运行时间长、历史代码多、模块关系复杂、文档不完整、人员持续变化的存量项目。**

---

# 1. 为什么需要 AEK

Coding Agent 已经能够非常快速地完成代码生成。

但在企业研发中，真正困难的问题往往已经不再是：

> AI 能不能把这段代码写出来？

而是：

- AI 是否真正理解当前项目？

- 找到的是不是实际业务入口？

- 这次修改会影响哪些模块？

- 是否遗漏了接口、数据库、消息、缓存或权限关系？

- 历史上为什么这样设计？

- 同样的问题以前是否出现过？

- AI 生成的设计和最终代码是否一致？

- AI 说“测试通过”，有什么证据？

- AI 说“任务完成”，谁来裁决？

- 换一个 Agent 或换一个会话后，项目知识是否又丢失？

在新项目里，这些问题可能暂时不明显。

但随着项目不断迭代：

```text
代码越来越多
     ↓
模块关系越来越复杂
     ↓
历史决策逐渐丢失
     ↓
文档与代码逐渐偏离
     ↓
修改越来越依赖少数熟悉系统的人
```

AI 如果没有工程约束，只会进一步放大这个问题：

```text
局部搜索
   ↓
局部理解
   ↓
快速修改
   ↓
潜在影响没有被发现
```

因此 AEK 的重点不是：

> 让 AI 写更多代码。

而是：

> **让 AI 在已有工程体系中更加可靠地工作。**

---

# 2. AEK 解决的核心问题

AEK 当前主要围绕五类问题展开。

## 2.1 老项目难以理解

存量项目通常存在：

- 历史代码规模大；

- 模块依赖复杂；

- 文档不完整或已经过期；

- 相似逻辑存在多个版本；

- 真实入口不容易判断；

- 大量设计原因只存在于开发人员经验中。

AEK 希望把：

```text
AI 搜到一段代码
       ↓
直接修改
```

改变为：

```text
理解任务
   ↓
恢复项目上下文
   ↓
定位真实入口
   ↓
分析调用关系
   ↓
检索历史项目知识
   ↓
核验当前代码与测试
   ↓
确定修改范围
```

---

## 2.2 老功能越来越不敢改

一个表面上很小的修改，可能同时影响：

```text
Controller
Service
Database
Cache
Message
API
SDK
Permission
Test
```

真正困难的是：

> **影响范围是否判断完整。**

AEK 通过代码关系、项目记忆、工程文档和当前源码共同恢复上下文，使 Agent 在修改之前先回答：

```text
入口在哪里？

调用了什么？

数据从哪里来？

谁依赖它？

有没有公共契约？

有哪些现有测试？

历史上有没有相关约束？
```

AEK 不把检索结果直接当成事实。

最终仍然要求回到：

```text
当前源码
+
当前测试
+
当前配置
```

进行核验。

---

## 2.3 复杂异常难定位

真实生产问题往往不是：

```text
NullPointerException
→ 修这一行
```

而更像：

```text
A Service
    ↓
B Service
    ↓
Gateway / Auth / MQ / DB
    ↓
C Service
```

其中：

```text
A → B 正常
B → C 异常
```

可能涉及：

- 调用链；

- 环境配置；

- 认证信息；

- 数据状态；

- 事务边界；

- 消息重复；

- 并发；

- 最近代码变化；

- 历史兼容逻辑。

AEK 不鼓励：

```text
看到异常
→ 猜一个原因
→ 改代码
→ 再试
```

而采用：

```text
现象
  ↓
证据收集
  ↓
调用链重建
  ↓
候选原因
  ↓
单一假设
  ↓
最小验证实验
  ↓
排除 / 确认
  ↓
根因
  ↓
回归测试
  ↓
最小修复
```

目标不是要求 AI 一次猜对。

而是：

> **让异常范围随着证据增加不断缩小。**

---

## 2.4 AI 开发过程缺少工程约束

传统 Coding Agent 工作方式很容易变成：

```text
AI 分析
↓
AI 设计
↓
AI 编码
↓
AI Review
↓
AI 告诉你“完成了”
```

这里最大的风险在于：

> 分析者、执行者和裁决者可能是同一个 AI。

AEK 将职责拆分为：

```text
AI
负责：
分析
生成
发现问题
提出建议

        ↓

确定性程序
负责：
结构检查
Hash 检查
追踪检查
门禁判断
规则执行

        ↓

人工
负责：
业务确认
需求冻结
关键设计决策
高风险事项
```

核心原则是：

> **AI 负责分析，规则负责裁决，人负责业务决策。**

---

## 2.5 项目知识持续流失

老项目真正重要的信息并不只在源码中。

还包括：

- 为什么这样设计；

- 为什么不能这样改；

- 历史兼容约束是什么；

- 哪些模块以前出现过问题；

- 哪些方案已经尝试过；

- 哪些操作容易产生事故。

这些信息往往散落在：

```text
设计文档
历史任务
代码注释
故障复盘
开发人员经验
历史聊天
版本记录
```

AEK 将这些信息逐步沉淀为：

> **可以检索、可以召回、可以重新核验的项目长期记忆。**

---

# 3. AEK 不是什么

AEK 不是：

- 一个新的 Coding Agent；

- Codex 或 Claude Code 的替代品；

- 一个“全自动开发软件”的平台；

- 一个要求所有修改都写大量文档的流程工具；

- 一个只面向新项目的 SDD 框架；

- 一个让 AI 自己决定所有事情的自治系统。

更准确地说，AEK 位于：

```text
                Human
                  │
                  ▼
        Codex / Claude Code
          / Other Agents
                  │
                  ▼
        ┌──────────────────┐
        │       AEK        │
        │                  │
        │ Engineering      │
        │ Control Layer    │
        └────────┬─────────┘
                 │
                 ▼
             Repository
```

Agent 仍然负责完成开发工作。

AEK 负责约束：

```text
做什么
做到什么程度
依据什么事实
需要什么设计
需要什么验证
什么情况下才能交付
```

---

# 4. AEK 的整体工作模型

AEK 当前希望建立这样一条闭环：

```text
                用户需求 / Bug
                       │
                       ▼
                最小安全路线判断
                       │
             ┌─────────┴─────────┐
             ▼                   ▼
       项目上下文恢复         异常根因定位
             │                   │
             └─────────┬─────────┘
                       ▼
                 需求与影响确认
                       │
                       ▼
                  必要的设计
                       │
                       ▼
                 实现与测试
                       │
                       ▼
                   交付门禁
                       │
                       ▼
              验证回执 / As-Built
                       │
                       ▼
                   项目记忆
                       │
                       └──────────────→ 下一次迭代
```

几个关键原则贯穿整个流程：

```text
Understand before change.

Evidence before conclusion.

Design only when necessary.

AI review is not final judgment.

Code and tests outrank stale memory.

Delivery is more than code completion.
```

---

# 5. Proportional Engineering：任务越小，流程越轻

AEK 不认为所有任务都应该经过完整 SDD。

当前通过不同路线控制工程流程重量：

```text
Direct
Bounded
Standard
Initiative
```

路线判断关注的是：

- 影响范围；

- 不确定性；

- 是否修改公共契约；

- 是否跨模块；

- 是否容易回滚；

- 风险等级。

而不是简单根据：

```text
Bug
Feature
Refactor
```

进行分类。

---

## 5.1 Direct

适用于范围明确、风险低、容易验证的小修改。

例如：

```text
文案修改
局部样式调整
简单配置修正
明确的小范围 Bug
```

目标流程：

```text
理解意图
↓
确认影响
↓
修改
↓
定向验证
```

不应该为了形式完整而生成整套设计文档。

---

## 5.2 Bounded

适用于边界明确，但仍需要一定分析和验证的修改。

例如：

```text
单模块行为调整
新增局部查询条件
小范围 API 行为修改
```

重点是记录必要决策和风险，而不是展开完整设计工程。

---

## 5.3 Standard

适用于存在明显设计工作、影响多个位置或需要较完整验证的需求。

通常会涉及：

```text
需求
必要设计
任务
测试
Review
实现
交付验证
```

---

## 5.4 Initiative

适用于：

- 跨模块改造；

- 公共契约变化；

- 大型功能；

- 高风险变更；

- 较高不确定性工作。

这类任务才默认进入较完整的 SDD 和交付闭环。

---

## 核心目标

AEK 的路线系统既要避免：

```text
复杂任务处理过轻
```

也要避免：

```text
小功能过度设计
```

最终目标是：

> **工程流程的重量与风险和不确定性成比例。**

---

# 6. Repository Understanding：先理解，再修改

AEK 对存量项目最重要的一项能力，是帮助 Agent 重建当前任务相关的工程上下文。

面对一个已有功能，Agent 首先需要回答：

```text
真实入口在哪里？

调用链是什么？

核心数据在哪里？

有哪些相似实现？

哪些测试覆盖了这里？

有哪些历史设计决策？

哪些地方属于潜在影响范围？
```

因此 AEK 将以下信息组合使用：

```text
项目文档
+
当前源码
+
测试代码
+
调用关系 / 代码图谱
+
项目长期记忆
+
已有同类实现
```

例如：

```text
修改 OrderService.confirm
```

不能只看：

```text
OrderService.java
```

而应该继续寻找：

```text
谁调用 confirm？
      ↓
confirm 调用了什么？
      ↓
是否操作数据库？
      ↓
是否发消息？
      ↓
是否涉及缓存？
      ↓
有哪些回归测试？
      ↓
历史上这里有没有特殊约束？
```

这样 AEK 希望将：

> **局部代码搜索**

提升为：

> **任务相关工程上下文恢复。**

---

# 7. Traceability：需求、设计、测试和实现能够互相追踪

AEK 当前建立了以下追踪结构：

```text
业务场景
   ↓
A-ID   Acceptance
   ↓
F-ID   Feature
   ↓
D-ID   Design
   ↓
TC-ID  Test Case
   ↓
T-ID   Task
```

目的不是为了增加编号。

而是建立可回答的问题：

```text
这个业务要求在哪里被实现？

这个功能对应哪个设计？

哪个测试证明这个要求已经验证？

这个任务为什么存在？

某段设计最终有没有落到实现？
```

追踪链由确定性工具进行检查，而不是完全依靠 Agent 自己声称：

> “这些应该已经覆盖了。”

最终希望形成：

```text
Requirement
    ↕
Design
    ↕
Test
    ↕
Task
    ↕
Implementation
```

---

# 8. AI Review + Deterministic Gate

AEK 不让 AI Review 直接决定工程状态。

Review 的职责是：

```text
发现问题
```

而不是：

```text
宣布通过
```

基本模型：

```text
Artifact
   ↓
AI Review
   ↓
Issues
   ↓
Deterministic Gate
   ↓
PASS / BLOCKED
```

AI Review：

- 不需要给总分；

- 不负责最终 PASS / FAIL；

- 重点输出结构化问题。

例如：

```text
blocker
major
minor
```

最终 Gate 再根据：

```text
文档结构
追踪完整性
Review Issues
Hash
上游状态
治理规则
```

进行判断。

---

## 为什么需要 Hash Binding

假设：

```text
Design V1
↓
Review PASS
```

之后有人修改：

```text
Design V2
```

如果 Review 仍然有效，就意味着：

> 一个旧版本的评审结果正在证明一个新版本是正确的。

AEK 因此将 Review / Freeze 状态与具体内容 Hash 绑定。

形成：

```text
Design V1
↓
Review V1
↓
Hash V1

Design 修改
↓
Hash V2
↓
旧 Review 自动失效
```

同样，上游冻结内容变化后，下游状态也可能需要重新确认。

这样：

> **评审结论属于某一个确定版本，而不是属于文件名。**

---

# 9. Evidence-Driven Debugging：异常定位基于证据而不是猜测

AEK 的调试原则是：

> 没有证据支持时，不要不断叠加修复。

典型流程：

```text
1. 稳定现象
      ↓
2. 收集事实
      ↓
3. 重建执行路径
      ↓
4. 区分候选原因
      ↓
5. 建立单一假设
      ↓
6. 最小实验
      ↓
7. 排除 / 确认
      ↓
8. 回归测试
      ↓
9. 最小修复
```

例如：

```text
支付回调导致库存被重复扣减
```

不能立即：

```text
if (...) return;
```

应该先区分：

```text
消息是否重复？
幂等逻辑是否失败？
是否存在并发？
事务是否回滚？
状态写入顺序是否错误？
```

每次验证只减少不确定性。

这种方式特别适合：

- 微服务调用问题；

- 权限和认证问题；

- MQ 问题；

- 并发问题；

- 数据不一致；

- 环境差异；

- 老版本兼容问题。

---

# 10. Semantic Project Memory：可语义检索的长期项目记忆

项目记忆是 AEK 面向存量项目的一项核心能力。

它不是简单的：

```text
把一些 Markdown 文件保存下来。
```

AEK 希望建立的是：

> **一个能够根据当前工程问题主动召回历史项目知识的长期记忆层。**

---

## 10.1 项目记忆解决什么问题

传统项目经常存在大量知识：

```text
为什么这个接口不能改？
为什么这里没有使用新组件？
以前发生过什么故障？
这个版本有什么兼容要求？
哪个模块有历史坑点？
```

但真正使用时往往不知道：

```text
在哪里
叫什么
用什么关键词搜索
```

AEK 将这些知识逐步结构化沉淀，例如：

```text
Decision
Pitfall
Playbook
Profile
业务域知识
```

并建立检索能力。

---

## 10.2 为什么需要语义检索

全文搜索擅长：

```text
OrderService
AUTH_TOKEN
REQ-017
某个错误码
```

但用户可能这样描述问题：

> B 服务调用 C 服务时认证失败。

历史经验却写成：

> 二次转发场景下服务凭证传播异常。

二者：

```text
关键词不完全相同
```

但：

```text
语义高度相关
```

因此项目记忆需要语义检索。

---

## 10.3 为什么又不能只依赖向量检索

软件工程中大量内容是精确符号：

```text
ClassName
MethodName
TableName
ConfigKey
ErrorCode
REQ-ID
```

这些信息往往：

```text
精确匹配 > 模糊语义
```

因此 AEK 采用混合检索思路：

```text
Semantic Recall
        +
Keyword / Anchor Recall
        +
Ranking Fusion
```

形成：

```text
当前问题
   ↓
语义召回
   ├────────────┐
   ↓            ↓
精确符号匹配   历史语义知识
   │            │
   └─────┬──────┘
         ↓
      融合排序
         ↓
   最相关项目记忆
```

---

## 10.4 为什么不把所有项目历史都交给模型

随着项目发展：

```text
历史设计越来越多
事故记录越来越多
决策越来越多
Pitfall 越来越多
```

如果全部送给 Agent：

```text
上下文巨大
+
无关信息增加
+
推理干扰增加
```

项目记忆采用：

```text
大量长期知识
      ↓
检索
      ↓
当前最相关的少量信息
      ↓
Agent Context
```

达到两个目标：

1. 控制 Agent 上下文规模；

2. 让项目知识数量增长后仍然能够复用。

---

## 10.5 Memory 不是最终事实

项目记忆可能过期。

因此 AEK 的事实优先级是：

```text
当前源码
当前测试
当前配置
当前运行事实
        >
历史 Memory
```

项目记忆负责：

> **告诉 Agent 哪些历史知识值得重新验证。**

而不是：

> **告诉 Agent 永远相信历史结论。**

例如历史记录：

```text
系统不支持 Feature X
```

但如果当前代码已经升级，

Agent 应重新核验代码，而不是继续按照旧 Memory 工作。

---

# 11. TDD：让实现结果可观察、可验证

AEK 将：

```text
代码写完
```

和：

```text
功能完成
```

区分开。

开发阶段强调：

```text
RED
 ↓
GREEN
 ↓
REFACTOR
```

重点不是单纯追求测试数量或覆盖率数字。

而是围绕：

> **业务上能够观察到的行为**

建立验证。

这样测试既是：

```text
开发反馈
```

也是：

```text
交付证据
```

同时还成为未来修改时的重要回归保护。

---

# 12. Security Review：高风险变更额外增加安全约束

不是所有任务都需要完整安全审查。

但涉及以下内容时，应增加额外安全分析：

- 认证；

- 授权；

- 敏感数据；

- 文件系统；

- SQL；

- 命令执行；

- SSRF；

- 支付回调；

- 依赖供应链；

- 外部输入。

因此安全能力仍然遵循：

> **按风险增加工程重量。**

---

# 13. Delivery Gate：代码完成不等于可以交付

AEK 区分：

```text
Implementation Complete
```

和：

```text
Delivery Ready
```

最终交付需要重新确认：

```text
实际修改是否超出预期范围？

治理规则是否满足？

Review 问题是否关闭？

要求执行的测试是否真的运行？

设计与最终实现是否一致？

是否留下足够验证证据？
```

## 13.1 性能与容量不是默认全开，而是风险触发

AEK 不要求每个 SQL 或小修改都执行计划分析与压测。普通 SQL/ORM 查询形状变化先做一次
`sql-performance-screen`：识别真实数据库与方言，检查 N+1、无界结果、分页/批次、已知索引
和明显的事务/锁风险。该初查不自动升径，也不把 PostgreSQL 的索引、锁和 DDL 语义套到
MySQL、Oracle、SQL Server、SQLite 等数据库。

只有初查发现规模风险，或改动触及以下范围时，Route Card 才增加
`performance_capacity`：

- 初查无法排除规模风险的 SQL / ORM 查询和数据库访问模式；

- schema、索引、约束、迁移和数据回填；

- 批处理、循环内 IO、远程调用扇出和大对象序列化；

- 缓存、锁、并发、队列背压和已知热点路径；

- 需求已经给出延迟、吞吐、资源或数据量目标。

升级后，`delivery-gate` 的 `performance-review` 才要求把静态审查升级为证据审查：

```text
权威预算 / 变更前基线
        ↓
代表性数据规模与参数分布
        ↓
实际 SQL、查询次数与目标数据库原生执行计划
        ↓
索引/基数/锁/IO/复杂度/竞争分析
        ↓
基准或端到端指标与门槛对照
```

初查只筛选风险，不证明运行时性能。进入深度阶段后，静态看到“有索引”或“复杂度应该不高”
仍不能替代代表性环境、数据或预算；缺少这些证据时，AEK 会明确给出
`NEEDS HUMAN REVIEW` 和补验步骤，而不会把未知状态写成通过。没有命中性能
风险的小修改仍走 Direct/Bounded 的针对性验证，不承担无关的性能仪式。

因此最终状态不是：

> Agent 说完成了。

而是：

> **实现、验证、设计和风险已经形成闭环。**

---

# 14. As-Built：设计最终应该描述“实际做成了什么”

传统设计文档通常记录：

> 准备怎么做。

但长期维护真正需要知道的是：

> 最后实际上怎么做了。

开发过程中设计可能合理发生变化：

```text
Design V1
    ↓
Implementation
    ↓
发现真实约束
    ↓
Implementation V2
```

如果最终文档仍然停留在 V1：

```text
文档开始失真
```

AEK 因此通过：

```text
验证结果
+
设计差异
+
最终实现
+
As-Built
```

完成收口。

使设计文档从：

```text
开发前的一次性材料
```

逐步变成：

```text
后续维护人员仍然可以信任的工程档案
```

---

# 15. Project-Level Governance：把个人 AI 技巧变成项目能力

如果完全依赖个人 Prompt：

```text
Developer A
→ 一套 Claude 使用方式

Developer B
→ 一套 Codex 使用方式

Developer C
→ 又一套流程
```

项目最终很难形成统一工程标准。

AEK 将：

- Skills；

- 路线判断；

- 项目规则；

- 文档门禁；

- 项目记忆；

- 调试方法；

- 测试方法；

- 交付要求；

安装到项目级。

最终希望实现：

```text
个人 AI 技巧
      ↓
项目工程能力
```

因此：

```text
换开发人员
换 Agent
换会话
```

项目规则仍然存在。

---

# 16. 当前能力与演进方向

为避免把规划能力和当前能力混为一谈，AEK 将能力分成两个层次。

## 当前已经形成的核心能力

当前体系已经覆盖：

```text
✓ 分级研发路线
✓ 老项目上下文恢复
✓ 代码关系 / 影响分析辅助
✓ 需求—设计—测试—任务追踪
✓ AI Review + Deterministic Gate
✓ Hash / 上游状态绑定
✓ Systematic Debugging
✓ 项目长期 Memory
✓ 语义 + 精确检索思路
✓ TDD
✓ Security Review
✓ Delivery Gate
✓ As-Built 收口
✓ 项目级统一安装和治理
```

---

## 后续演进方向

后续能力重点围绕现有体系继续演进，而不是改变核心定位。

### 更快

```text
增量上下文恢复
缓存
选择性重新验证
并行只读分析
```

### 更自动

```text
任务状态结构化
自动 Workflow
Subagent
Resume
安全并行执行
```

### 更高质量的设计产物

```text
结构化 Artifact
更严格设计 Review
设计事实与 Evidence 联动
```

### 企业归档

```text
标准模板
DOCX / PDF
Archive Package
完整追踪关系
```

这些属于后续演进方向，不应与当前已实现能力混淆。

---

# 17. 典型使用场景

## 17.1 修改一个维护多年的老功能

例如：

> 给已有用户查询增加一个筛选条件。

传统方式：

```text
搜索 Controller
↓
搜索 Service
↓
尝试修改
```

AEK 更关注：

```text
恢复真实业务入口
↓
找到已有查询模式
↓
判断数据来源
↓
分析查询、导出等复用路径
↓
检查历史约束
↓
确定最小修改范围
↓
完成实现
↓
回归验证
```

---

## 17.2 定位跨服务线上异常

例如：

```text
A → B 正常
B → C 返回 401
```

AEK 关注：

```text
还原调用路径
↓
确认失败发生在哪一跳
↓
检查认证信息传播
↓
检查配置差异
↓
查看最近相关改动
↓
检索历史 Incident / Pitfall
↓
提出最小假设
↓
验证
```

而不是：

```text
先改一个认证逻辑看看。
```

---

## 17.3 完成一个中等规模需求

典型链路：

```text
需求理解
↓
路线判断
↓
需求确认
↓
必要设计
↓
Review
↓
任务
↓
实现
↓
测试
↓
Delivery Gate
↓
As-Built
↓
Memory
```

下一次：

```text
换 Agent
```

仍然能够从项目资产继续工作。

---

# 18. 什么项目最适合 AEK

AEK 尤其适合：

## 长期维护型项目

特点：

- 代码历史长；

- 开发人员变化多；

- 项目经验容易流失。

---

## 模块关系复杂的项目

例如同时包含：

```text
Gateway
Microservices
MQ
Database
Cache
Authentication
```

---

## Bug 定位成本较高的项目

尤其是：

> 很多问题必须依赖某几个“老开发”才能快速定位。

---

## 有正式设计和交付要求的项目

例如需要：

- 技术评审；

- 交付归档；

- 项目交接；

- 版本审计。

---

## 已经大量使用 Coding Agent 的团队

AEK 并不是为了说服：

```text
完全不用 AI 的团队
```

开始使用 AI。

它更适合：

```text
Coding Agent 已经大量进入开发
```

之后产生的新问题：

```text
如何统一
如何约束
如何验证
如何积累
```

---

# 19. 如何评价 AEK 是否真正有效

不建议使用以下指标衡量：

```text
生成多少文档
用了多少 Agent
产生多少代码
```

这些指标很容易变成形式主义。

更值得关注的是：

## 迭代开发

- 找到正确修改位置需要多久；

- 无关代码修改是否减少；

- 影响范围遗漏是否减少；

- 回归问题是否减少；

- AI 人工纠偏次数是否减少；

- 设计与实现偏差是否减少。

---

## 异常定位

- 从收到问题到缩小范围需要多久；

- 无证据修改次数；

- 根因定位耗时；

- 同类问题重复排查次数；

- 历史知识复用次数。

---

## 项目知识

- 历史设计、Decision 和 Pitfall 的实际命中率；

- 新成员理解已有功能的时间；

- 换人或换 Agent 后重新分析比例；

- 历史问题是否能够被下一次任务复用。

AEK 不预设：

> 一定提升 XX%。

实际收益应该通过真实项目持续验证。

---

# 20. AEK 的核心设计原则

AEK 当前遵循以下原则。

## 20.1 Understand Before Change

在存量系统中：

> 先理解，再修改。

---

## 20.2 Evidence Before Conclusion

尤其是问题定位：

> 没有证据，不把猜测包装成结论。

---

## 20.3 Proportional Engineering

> 工程流程重量与风险、影响范围和不确定性成比例。

---

## 20.4 AI Is a Sensor, Not the Final Judge

AI 可以：

```text
发现问题
分析问题
提出建议
```

但关键门禁不能完全依赖：

```text
AI 自我评价
```

---

## 20.5 Code Is the Current Truth

历史 Memory 很重要。

但发生冲突时：

```text
Current Code
Current Tests
Current Configuration
Current Runtime Evidence
        >
Historical Memory
```

---

## 20.6 Traceability Over Assumption

不要依靠：

> “应该已经覆盖。”

而是尽量建立：

```text
Requirement
↓
Design
↓
Test
↓
Task
↓
Implementation
```

的显式关系。

---

## 20.7 As-Built Over Stale Design

设计不应该只描述：

> 原来准备怎么做。

最终还应该说明：

> 实际是怎么做的。

---

# 21. AEK 希望最终带来的变化

AEK 希望逐步把软件工程中的 AI 使用方式从：

```text
AI 是个人开发助手
```

变成：

```text
AI 是项目工程体系中的一个受控参与者
```

从：

```text
每次会话重新理解项目
```

变成：

```text
项目知识持续积累并可检索
```

从：

```text
AI 根据局部代码快速修改
```

变成：

```text
理解上下文和影响范围后再修改
```

从：

```text
复杂问题依赖个人经验
```

变成：

```text
基于证据逐步定位
+
历史经验可复用
```

从：

```text
设计、代码、测试逐渐分离
```

变成：

```text
需求
↕
设计
↕
实现
↕
验证
↕
As-Built
```

---

# 22. 最终目标

AEK 最终追求的并不是：

> 让 AI 自动完成所有开发工作。

而是：

> **让项目随着不断迭代积累更多可复用的工程知识，而不是积累更多只有少数人能够理解的历史包袱。**

同时：

> **让 Coding Agent 使用规模越大，研发过程仍然能够保持可理解、可验证、可追踪和可控制。**

可以将整个目标概括为：

```text
Understand
    ↓
Locate
    ↓
Change
    ↓
Verify
    ↓
Remember
```

对于存量项目：

> **理解正确，比写得快更重要。**

对于 AI 工程：

> **自动化越强，工程边界越需要清晰。**

这就是 Agent Engineering Kit 希望解决的问题。**
