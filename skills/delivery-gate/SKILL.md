---
name: delivery-gate
description: "交付门禁，两种形态：代码变更走 diff 证据化审查+对抗复核+验证闭环+回执；设计文档（需求澄清/业务流程设计/详细设计/任务清单）走结构/字段/一致性/可验证性审查+回执。验证回执是声称验证通过的唯一证据形态；两种形态共用结论枚举 READY / NOT READY / NEEDS HUMAN REVIEW。"
---

# 交付门禁

零额外依赖的收口门禁，按交付物形态执行：**代码变更**走两阶段（审查 → 验证闭环），**设计文档**走文档质量门禁。外部 review CLI 可作为可选增强，但不可成为基础流程的隐式要求。

## 代码变更形态

### 阶段一：最终 diff 审查

1. 固定本次任务目标、验收条件、基线和最终 diff；区分本次修改与用户已有改动。
2. 至少逐项检查：行为正确性与失败路径、项目宪章/规范、调用影响与兼容性、测试充分性、复杂度与维护性、文档/记忆一致性。
3. 命中安全触发条件时调用 `security-review`，并把其结果纳入同一门禁。
4. 每个发现给出准确文件/符号、可观察后果、触发条件、证据和最小修复方向；按 `CRITICAL/HIGH/MEDIUM/LOW` 标级。纯偏好不作为阻塞项。

#### 对抗式复核

对 HIGH/CRITICAL 和会阻止交付的结论，另做一次"证明它是误报"的复核：检查不可达分支、上游校验、事务边界、部署配置和已有测试。运行环境提供子 Agent 时，把 diff、验收条件与本轮发现交给一个**未参与实现的独立审查 Agent** 复核；不可用时在同一 Agent 中清空假设做第二遍，并在回执中披露这是同上下文复核。

只保留证据和置信度足够的发现，重复问题合并为根因项。任何审查维度未完成、工具异常或证据相互矛盾时 fail closed：结论只能更严，不得把缺失检查当作通过。

### 阶段二：验证闭环

先从项目宪章、根指令、CI 和构建文件提取真实门禁，不硬编码语言、覆盖率或工具。按项目与变更适用性运行，先快后慢：

1. 构建、编译或类型检查；
2. 格式化、lint 和静态分析；
3. 针对性测试、相关模块测试、必要的全量/集成/契约/并发/性能测试；
4. `security-review` 要求的扫描与安全回归；
5. spec/SDD、变更记录、用户文档和项目记忆的一致性检查。

命令失败或行为异常时进入 `systematic-debugging`；不得跳过、放宽断言或改写验收条件来制造绿色。因环境、权限或缺少依赖无法运行时，在回执中记录准确原因、影响和人工补验步骤。阶段一的修复必须重跑直接回归后重新过门，不能仅凭 diff 看起来合理就关闭。

## 设计文档形态（设计阶段质量门禁）

编码前的设计产出——②需求澄清结论、③《业务流程设计》、④《详细设计》、⑤任务清单——在进入下一阶段或人工确认前过同一门禁，检查四维：

1. **结构完整性**：对照骨架逐节核查——概要设计（功能点清单含菜单/页面落位与功能点关系、交互流程、原型或页面内容清单、技术概要）、业务流程设计（简介[目的/范围/术语]/需求描述/总体设计[架构+业务闭环]/功能设计/数据结构/接口设计，团队双文档惯例时）、详细设计（目标与边界/总体链路/领域模型/分侧设计/数据库/接口/验收要点）、实现计划（纵向可验证任务+每任务验证方式+引用编码规范）；项目自定义设计模板优先于默认骨架。
2. **字段覆盖**：按统一产出字段的阶段标注核对——设计/契约/数据流、兼容性与风险、安全与威胁模型须在③④成型，测试接缝与用例在④⑤；缺失即逐项列出；「待确认」必须显式标注且注明确认方，不得冒充已完成内容。
3. **一致性**：详细设计不推翻概要已确认结论（发现冲突即阻断并列出待裁决）；**验收要点必须能逐条溯源到②确认的需求场景与产出**——无场景依据的验收项即缺口；设计文档互相引用可达；与需求文档、业务域地图口径一致；跨文档引用的路径有效。
4. **可验证性**：验收条件可测量（能判 PASS/FAIL）；每个实现任务带验证方式；失败、边界与回退场景已覆盖。

**对抗式复核**：高风险设计决策（公共契约、数据模型、兼容性、安全）做一次"证明它错"的第二遍——与代码形态同规则（独立子 Agent 优先，否则清空假设并披露同上下文）。

结论与代码形态同一套枚举：`READY` / `NOT READY`（即需补充，逐项给出章节与缺口描述） / `NEEDS HUMAN REVIEW`。**回执频率**：②③ 的产出做快速审查，结论可并入④的定稿回执，不强制单独落盘；④ 设计定稿必须落独立回执，门禁执行表按上述四维逐项列检查与结果。轻量路线（短路/简版）无设计文档时不跑本形态。

## 验证回执（必需产物）

验证结论必须落持久回执，这是**声称验证通过的唯一证据形态**——对话内叙述的验证结果不构成完成依据。回执写入 `docs/delivery-receipts/YYYY-MM-DD-<中文任务名>.md`（目录不存在则创建）：

```markdown
---
type: receipt
created: YYYY-MM-DD HH:MM
task: <一句话任务>
scope: <变更范围（文件/模块）>
verdict: READY / NOT READY / NEEDS HUMAN REVIEW
form: code / design
review: <审查结论；对抗复核为独立 Agent 或同上下文>
---

# 交付验证回执：<任务>

## 门禁执行

代码形态：检查=命令与退出码；设计文档形态：检查=四维（结构/字段/一致性/可验证性）逐项。

| 检查 | 方式/命令 | 结果 |
| --- | --- | --- |
| 构建 | <命令>（退出码 <code>） | <一句话> |
| 结构完整性 | 对照骨架逐节核查 | <结论/缺口> |
| 一致性 | 跨文档引用与上游结论比对 | <结论/缺口> |

未执行项必须写明原因与人工补验步骤，不得留空。

## 审查发现与处置

| 严重性 | 位置 | 证据 | 处置（已修复 / 留档 / 误报-复核依据） |
| --- | --- | --- | --- |

## 结论

<READY / NOT READY / NEEDS HUMAN REVIEW 及依据；人工审核项逐条列出>
```

只有适用门禁全部通过、没有未裁决的阻塞发现、代码与权威文档无已知冲突时才可 `READY`。项目要求人工审核时必须 `NEEDS HUMAN REVIEW`，即使自动检查全绿。回执落盘后不改写历史行；重跑门禁就写新回执。

## 来源

多维审查、证据门槛、skeptic 复核与分阶段验证闭环思路参考并改编自 [Everything Claude Code 的 orch-review workflow 与 verification-loop](https://github.com/affaan-m/ECC)，Copyright 2026 Affaan Mustafa，MIT License。
