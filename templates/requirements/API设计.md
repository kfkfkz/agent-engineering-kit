# API设计：〈需求名称〉

> 实现人撰写。与 详细设计.md、数据库设计.md 同属"详细设计"阶段——本文件管
> 接口契约（D-ID 三文档内全局唯一）。对外接口必含错误与幂等。
> 不适用项写"不适用 + 原因"，不删节。

## 接口清单

| 编号 | 接口 | 类型 | 变化 | 关联功能点 |
| --- | --- | --- | --- | --- |
| D2 | 〈接口名〉 | 新增/修改 | 〈一句话〉 | F1 |

## 接口详细设计

### D2：〈接口名称〉（对应 F1 / A1）

- 协议与路径：〈HTTP `POST /x` / RPC / 内部方法〉
- 权限：〈谁可调用〉
- 请求字段：〈字段 | 类型 | 必填 | 约束 | 说明〉
- 响应字段：〈字段 | 类型 | 说明〉
- 错误与幂等：〈错误码表；重复提交行为〉
- 兼容性：〈对既有调用方的影响〉
