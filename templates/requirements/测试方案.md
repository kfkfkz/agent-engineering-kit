# 测试方案：〈需求名称〉

> 实现人撰写（可由 tdd 技能按详细设计生成后补充）。生成前提：详细设计.md 已冻结。
> 每条用例必须挂到验收项——不挂在 A 上的用例不算覆盖。不适用项写"不适用 + 原因"。

## 测试接缝

| 层级 | 接缝 | 说明 |
| --- | --- | --- |
| 单元 | 〈服务/纯函数〉 | 〈覆盖的核心规则〉 |
| 集成 | 〈接口/存储〉 | 〈契约与权限路径〉 |

## 用例清单

> "对应验收项"列必填 A-ID；预期必须是可观察结果。

| ID | 用例 | 对应验收项 | 层级 | 前置数据 | 预期（可观察） | 自动化 |
| --- | --- | --- | --- | --- | --- | --- |
| TC-1 |  | A1 |  |  |  | 是/否 |
| TC-2 |  | A2 |  |  |  | 是/否 |

## 边界与异常覆盖

> 至少考虑以下各项；不适用的写"不适用 + 原因"，不许留空。

- 空值 / 极值 / 非法状态：
- 重复调用 / 并发 / 超时：
- 历史数据（迁移后的存量）：
- 数据库 / 外部依赖 / 网络 / 权限 / 配置异常：
- 重试耗尽后的最终行为：

## 环境与数据

〈测试环境、账号、造数与清理方式；失败时的日志点与错误码。
**禁止保存真实密码/Token/Secret**〉

## 回归范围

| 模块 | 回归原因 | 回归方式 |
| --- | --- | --- |
|  |  |  |

## 通过标准

- [ ] 每个验收项至少一条验证路径（doc-gate 追踪检查）
- [ ] 全部用例通过且证据留存
- [ ] 回归测试通过
