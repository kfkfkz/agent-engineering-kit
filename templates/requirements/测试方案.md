# 测试方案：〈需求名称〉

> 实现人撰写（可由 tdd 技能按详细设计生成后补充）。生成前提：详细设计.md 已冻结。
> 每条用例必须挂到验收项——不挂在 A 上的用例不算覆盖。不适用项写"不适用 + 原因"。

## 测试接缝

| 层级 | 接缝 | 说明 |
| --- | --- | --- |
| 单元 | 〈服务/纯函数〉 | 〈覆盖的核心规则〉 |
| 集成 | 〈接口/存储〉 | 〈契约与权限路径〉 |

## 用例清单

> "对应验收项"列必填 A-ID；预期必须是可观察结果。

| ID | 用例 | 对应验收项 | 层级 | 前置数据 | 预期（可观察） | 自动化 |
| --- | --- | --- | --- | --- | --- | --- |
| TC-1 |  | A1 |  |  |  | 是/否 |
| TC-2 |  | A2 |  |  |  | 是/否 |

## 边界与异常覆盖

> 至少考虑以下各项；不适用的写"不适用 + 原因"，不许留空。

- 空值 / 极值 / 非法状态：
- 重复调用 / 并发 / 超时：
- 历史数据（迁移后的存量）：
- 数据库 / 外部依赖 / 网络 / 权限 / 配置异常：
- 重试耗尽后的最终行为：

## 环境与数据

〈测试环境、账号、造数与清理方式；失败时的日志点与错误码。
**禁止保存真实密码/Token/Secret**〉

## 回归范围

| 模块 | 回归原因 | 回归方式 |
| --- | --- | --- |
|  |  |  |

## 性能与容量验证

- SQL 初查：〈sql-performance-screen 结果；无升级风险则写依据并结束〉
- 深度适用性：〈命中 performance_capacity 的证据；未命中则不要求执行计划/压测〉
- 基线/目标：〈项目预算或可复现的变更前基线，不得虚构阈值〉
- 代表性数据规模：〈行数/对象大小、参数分布、并发、冷/热缓存〉
- 验证方式：〈查询次数与 N+1、执行计划、基准/负载/剖析命令〉
- 留证指标：〈适用的 p50/p95/p99、吞吐、CPU/内存/IO、锁/临时文件〉
- 通过/阻断条件：〈满足目标；缺环境/数据时转人工补验，不以静态判断代替〉

## 通过标准

- [ ] 每个验收项至少一条验证路径（doc-gate 追踪检查）
- [ ] 全部用例通过且证据留存
- [ ] 回归测试通过
- [ ] 命中性能/容量风险时，运行证据满足预算或已明确阻断/人工补验
